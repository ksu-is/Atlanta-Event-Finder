# Atlanta-Event-Finder
Atlanta Event Finder is a Flask-based web app that helps users find events happening in Atlanta. It uses BeautifulSoup to scrape event data from websites and stores it in a SQLite database. Users can browse events, filter by category or date, view details, and save their favorite events, making it easier to discover things to do in one place.
